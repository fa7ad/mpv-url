#!/bin/bash

# delete "mpv://"
request=${1:6}
# "decode" uri components
decoded=$(printf '%b' "${request//%/\\x}")

# launch mpv
if [ -x "$(command -v mpvh)" ]; then
  mpvh "$decoded"
else
  mpv "$decoded"
fi